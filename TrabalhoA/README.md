# 🏥 Instruções de Compilação e Execução - Trabalho A (Triagem Hospitalar)

Este projeto foi desenvolvido utilizando uma estrutura modularizada (contendo os arquivos `main.c`, `triagem.c` e `triagem.h`). Para que o programa funcione corretamente, é obrigatório compilar todos os arquivos de código-fonte juntos.

## 1. Como Compilar

Abra o terminal (linha de comando ou terminal integrado do VS Code), certifique-se de estar dentro da pasta onde os arquivos do Trabalho A estão salvos e execute o seguinte comando:

```bash
gcc *.c -o triagem
