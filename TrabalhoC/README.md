# 🖨️ Instruções de Compilação e Execução - Trabalho C (Fila de Impressão)

Este projeto foi desenvolvido utilizando uma estrutura modularizada (contendo os arquivos `main.c`, `fila.c` e `fila.h`). Para que o programa gerencie corretamente a fila normal e a fila prioritária, é obrigatório compilar todos os arquivos de código-fonte juntos.

## 1. Como Compilar

Abra o terminal (linha de comando ou terminal integrado do VS Code), certifique-se de estar dentro da pasta onde os arquivos do Trabalho C estão salvos e execute o seguinte comando:

```bash
gcc *.c -o fila